# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars
*.tfvars.json

# Сохранённые планы: внутри zip лежит копия стейта, а в ней — ключи доступа
tfplan
*.tfplan

# Локальная конфигурация бэкенда (в репозитории есть backend.hcl.example)
backend.hcl
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
.terraform.lock.hcl

# Credentials
*.json
!kubernetes/**/*.json
key.json
sa-key.json
.env

# OS
.DS_Store
